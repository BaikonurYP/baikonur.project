const staticCacheName = 's-app-v1'

const assetUrl = ['/index.html']

this.addEventListener('install', (event) => {
    console.log('install')
})

this.addEventListener('activate', (event) => {
    console.log('activate')
})
