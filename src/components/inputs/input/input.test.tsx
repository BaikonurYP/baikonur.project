import React from 'react'
import { render } from '@testing-library/react'
import Input from './input'

describe('Input test', () => {
    it('should render', () => {
        render(<Input />)
    })
})
